__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store
# Stray personal files that occasionally land in the repo root.
HotelBill_*.pdf
*.private.*
# MSA shutter V2/V3 grid is *required* to run vMPT; keep it tracked
# (now under vmpt/data/, was data/ before v1.2.2).
# Operability JSON is fetched from CRDS; not redistributed.
vmpt/data/jwst_nirspec_msaoper_*.json
data/jwst_nirspec_msaoper_*.json
exports/
# pip-build artefacts.
build/
dist/
*.egg-info/
.eggs/

# Sphinx output.
docs/_build/
docs/_autosummary/
# Example data is now committed (after JPG recompression in May 2026)
# so cloners can try vMPT immediately. If you don't want the large
# example files in your local copy, run:
#   git update-index --skip-worktree example_a370 example_r0600
refs/
.venv*/
*.egg-info/
# Slide-deck build artefacts (source is build_vmpt_slide.js, kept).
vmpt_summary.pptx
vmpt_summary.pdf
slide-*.jpg
vmpt-presentation.pptx
