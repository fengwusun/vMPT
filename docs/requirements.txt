# Documentation build deps for Read the Docs.
# Runtime deps (astropy, bokeh, jwst, …) are mocked via
# `autodoc_mock_imports` in docs/conf.py so RTD's container doesn't
# have to install them.
sphinx>=7.0
furo>=2024.5.6
myst-parser>=2.0
linkify-it-py>=2.0
sphinx-design>=0.5
