# API reference

The reference below is autogenerated from the docstrings in
`vmpt/`. Most users only need the `vmpt.optimizer` module — the
rest are the building blocks the Bokeh app drives internally.

## vmpt.optimizer

The MSA-pointing search + the shutter-collision protection rules.

```{eval-rst}
.. automodule:: vmpt.optimizer
   :members:
   :undoc-members:
   :show-inheritance:
```

## vmpt.msa

Loads the per-quadrant shutter centres in V2/V3 and parses CRDS's
`msaoper` JSON for operability + stuck-open flags.

```{eval-rst}
.. automodule:: vmpt.msa
   :members:
   :undoc-members:
```

## vmpt.wavelengths

Per-shutter wavelength endpoints for every supported
(disperser, filter) combination, plus the V2 overlap distance used
by the collision-protection rules.

```{eval-rst}
.. automodule:: vmpt.wavelengths
   :members:
   :undoc-members:
```

## vmpt.catalog

Loader + in-memory `Catalog` dataclass.

```{eval-rst}
.. automodule:: vmpt.catalog
   :members:
   :undoc-members:
```

## vmpt.catalog_ops

Helpers for deriving weight ↔ priority used by the catalog editor.

```{eval-rst}
.. automodule:: vmpt.catalog_ops
   :members:
   :undoc-members:
```

## vmpt.coords

Coordinate-frame helpers (V2/V3 ↔ RA/Dec, shutter polygons, PA
rotation matrix).

```{eval-rst}
.. automodule:: vmpt.coords
   :members:
   :undoc-members:
```

## vmpt.empt_io / vmpt.mpt_io / vmpt.session_io / vmpt.image_io

I/O modules — eMPT bundle writer, APT MPT plan reader, vMPT
workspace save/load, and image (FITS / JPG + sidecar) loaders
respectively.

```{eval-rst}
.. automodule:: vmpt.empt_io
   :members:

.. automodule:: vmpt.mpt_io
   :members:

.. automodule:: vmpt.session_io
   :members:

.. automodule:: vmpt.image_io
   :members:
```
